# Phase 4 · 集成测试 + 部署

> 时间：第 8 周
> 目标：全链路测试，性能达标，准备上线
> 状态：⚪ 待开始

---

## 功能测试矩阵

| 测试场景 | 预期结果 |
|---------|---------|
| 10 并发抢最后 1 个名额 | 仅 1 人成功，其余返回 1001 |
| 同手机同日期重复提交 | 第 2 次返回 1003 |
| 同 idempotency_key 重复请求 | 返回原订单号（code 1005）|
| Redis 宕机时提交订单 | 降级走 MySQL，正常返回 |
| 管理员设置休息日后 60s 内 | 前端刷新后显示灰色 |
| 取消订单后名额 | booked_slots 立即 -1 |
| 管理员 Token 过期 | 401 → 跳转登录，路径保留 |
| 小程序 session_key 过期 | 静默重新登录 |
| 小程序冷启动 | 日历正常加载，无白屏 |
| 网络超时（>5s）| 提示超时，表单保留 |

## 性能测试

```bash
# 日历接口：100 并发，P99 ≤ 1s
npx autocannon -c 100 -d 30 http://localhost:3000/api/schedule/calendar

# 订单提交：50 并发，零超卖
npx autocannon -c 50 -d 10 -m POST ... http://localhost:3000/api/order/submit
```

## 安全检查

- [ ] 手机号脱敏（138****0000）
- [ ] SQL 注入防护（Prisma 参数化）
- [ ] 管理后台 CSRF Token
- [ ] 客户端限速（同 IP ≤ 10/min）
- [ ] JWT 过期 + 篡改检测
- [ ] AppSecret 仅存服务端
- [ ] `wx.setStorageSync` 不存敏感信息
- [ ] 小程序合法域名配置正确

---

## 交付物

- [ ] 测试报告（功能 + 并发 + 安全）
- [ ] 小程序审核提交
- [ ] 上线部署文档
- [ ] 监控告警配置
