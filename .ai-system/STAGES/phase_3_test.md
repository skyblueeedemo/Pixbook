# Phase 3 · 管理后台

> 时间：第 6–7 周
> 目标：实现管理员全部操作界面
> 状态：⚪ 待开始

---

## 任务清单

### 第 6 周：排期管理 + 订单列表

| 任务 | 说明 |
|------|------|
| 管理员登录页 + JWT 存储 | localStorage + Axios 拦截器 |
| Token 过期自动跳转登录 | 401 响应拦截器 |
| `ScheduleCalendar.vue` | 月历视图，色块饱和度 |
| 设置每日上限弹窗 | 实时更新，失效 Redis |
| 添加休息日弹窗 | 日期选择器 + 确认 |
| 特定日期名额覆盖 | 点击日格 → 弹窗 |
| `OrderList.vue` | 表格 + 状态 Tab + 分页 |

### 第 7 周：订单详情 + 状态操作

| 任务 | 说明 |
|------|------|
| `OrderDetail.vue` | 侧滑面板 |
| `StatusFlow.vue` | 状态流转进度条 + 操作按钮 |
| 状态变更 API 对接 | `PATCH /api/admin/order/:id/status` |
| 二次确认弹窗 | 取消订单时弹窗 |
| 取消订单名额释放 | booked_slots -1，失效缓存 |

---

## 交付物

- [ ] 管理后台全流程可用
- [ ] 订单状态流转端到端测试通过
- [ ] 管理员鉴权安全测试通过（未授权返回 401）

---

## 状态流转

```
待确认 ──→ 修图中 ──→ 待交付 ──→ 已完成
  │                                 ↑
  └────────── 已取消 ←──────────────┘
        （任意阶段可取消，名额自动 +1）
```
