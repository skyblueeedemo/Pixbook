# Phase 3 · 管理后台

> 时间：第 6–7 周
> 目标：实现管理员全部操作界面
> 状态：🟢 已完成（2026-05-24）

---

## 任务清单

### 管理后台功能 ✅

| 任务 | 说明 | 状态 |
|------|------|:--:|
| 管理员登录页 + JWT | localStorage + Axios 拦截器 + 路由守卫 | ✅ |
| Token 过期自动跳转 | 401 响应拦截器 redirect | ✅ |
| `AdminLayout.vue` | 共享 header + sidebar 布局 | ✅ |
| `OrdersView.vue` | 表格 + 状态筛选 + 关键字搜索 + 日期范围 + 分页 | ✅ |
| `OrderDetail` 侧滑抽屉 | el-drawer + 订单信息 + 状态流转按钮 | ✅ |
| 状态流转 | 待确认→修图中→待交付→已完成 / 任意阶段→取消 | ✅ |
| 二次确认弹窗 | `ElMessageBox.confirm` | ✅ |
| 取消订单名额释放 | `GREATEST(booked_slots - 1, 0)` + Redis 失效 | ✅ |
| `ConfigView.vue` | 每日上限 / 预约天数 / 休息日勾选 / 保存 | ✅ |
| `DashboardView.vue` | 总订单 / 待处理 / 最近订单 | ✅ |
| 后端 `GET /api/admin/orders` | 筛选 + 分页 + 关键字搜索 | ✅ |
| 后端 `PATCH /api/admin/orders/:id/status` | 状态更新 + 名额释放 | ✅ |
| ScheduleView 排期管理 | — | ⬜ 后续迭代 |

---

## 交付物

| 交付物 | 验收标准 | 状态 |
|--------|---------|:--:|
| 管理后台全流程 | 登录→列表→详情→改状态→返回 | ✅ |
| 状态流转端到端 | 点击按钮→确认→刷新列表 | ✅ |
| 鉴权安全 | 401 → 跳转登录 | ✅ |

---

## 状态流转（已实现）

```
待确认(0) ──→ 修图中(1) ──→ 待交付(2) ──→ 已完成(3)
  │              │                │
  └──→ 已取消(4) ←──┘ ←──────────────┘
         （任意阶段可取消，名额自动 +1）
```
